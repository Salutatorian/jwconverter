# v0.2.8 batch — design

**Version:** v0.2.8  
**Status:** Approved (user requested full optional backlog in one ship)  
**Date:** 2026-07-26

## Goal

One release covering the remaining optional polish from the post-0.2.7 backlog.

## In scope

1. **Soak** — Unicode/long paths, cancel mid-batch smoke, corrupt inputs; fix blockers only.  
2. **Empty-state UX** — clearer DropZone / idle copy for Audio and Images.  
3. **Screenshot** — refresh README asset to show dual-mode UI.  
4. **HEIC honesty** — this Magick build is `HEIC r--` only; do **not** fake HEIC export. Surface a clear UI/about note that HEIC is import-only.

## Out of scope (blocked)

- HEIC **export** until a write-capable Magick/libheif is bundled.

## Success

- Signed **v0.2.8** published once with combined notes.
